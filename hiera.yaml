---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Node specific data"
    path: "nodes/%{trusted.certname}.yaml"

  - name: "Common data"
    path: "common.yaml"

  - name: "Secrets (last – overrides possible)"
    path: "secrets/dynatrace.yaml"
